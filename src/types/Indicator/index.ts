export type IndicatorSelectorProps = {
  selected: string;
  onChange: (value: string) => void;
};

export type SubIndicatorSelectorProps = {
  selected: string;
  onChange: (value: string) => void;
};
